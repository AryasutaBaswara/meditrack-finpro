{
  "name": "meditrack-automation",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright smoke tests and Locust load tests for MediTrack staging",
  "scripts": {
    "test:smoke": "playwright test --config=playwright.config.ts",
    "test:smoke:headed": "playwright test --config=playwright.config.ts --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
