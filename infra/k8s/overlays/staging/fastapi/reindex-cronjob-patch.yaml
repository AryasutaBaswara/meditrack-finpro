apiVersion: batch/v1
kind: CronJob
metadata:
  name: fastapi-reindex
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: reindex
              envFrom:
                - configMapRef:
                    name: staging-fastapi-config
                - secretRef:
                    name: staging-fastapi-secrets
